  cat_map_output = map_iterator(x,random_t)

  perturbed_x = x + cat_map_output


  score = model(perturbed_x, random_t)
  #print("score",score)
  loss = torch.mean(torch.sum((score * std[:, None, None, None] + z)**2, dim=(1,2,3)))

loss - 784%
