  cat_map_output = map_iterator(x,random_t)
  normalized_cmap = torch.norm(cat_map_output).mean()

  
  perturbed_x = x + (z * std[:, None, None, None]) * (normalized_cmap)


  loss = torch.mean(torch.sum((score * std[:, None, None, None] + z)**2, dim=(1,2,3)))

2.44 % loss
